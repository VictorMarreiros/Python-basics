# o uso do asterisco define que a variavél <*head> terá
# todo o conteúdo da lista exceto um elemento que será
# deixado para que a outra variavel <tail> não fique vazia   
*head, tail = [1, 2, 3, 4, 5]
print(head)
print(tail)


#head, *tail = [1, 2, 3, 4, 5] --> seria o inverso